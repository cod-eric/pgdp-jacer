public class SnakeLike {
    /*
        This is a pure helper class to allow Egg.incubate() to return either an Egg or a Snake.
        Since Java always needs the same return type, both Snake and Egg inherit SnakeLike, which
        is then returned by incubate().
     */
}
